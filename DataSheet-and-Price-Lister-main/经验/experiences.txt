
在控制台输入如下：
let elem = $x('//*[@id="textfield-1333-inputEl"]')[0];
console.log(elem.ownerDocument.defaultView.frameElement);
如果返回一个 <iframe> 元素，说明此输入框在 iframe 里
返回 null 说明不在 iframe

### 获取iframe的父元素
let elem = $x('//*[@id="textfield-1333-inputEl"]')[0];
console.log(elem.ownerDocument.defaultView.frameElement.parentElement);

### 获取html的结构
print(f"开始保存当前页面内容")
        with open("current_page.html", "w", encoding="utf-8") as f:
            f.write(driver.page_source)


### 在控制台 复制 html文本
1. copy(document.documentElement.outerHTML)     --复制整个html
2. 粘贴在.html文件中，保存即可